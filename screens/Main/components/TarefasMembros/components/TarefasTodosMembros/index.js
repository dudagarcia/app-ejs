import React from 'react';
import { View, ScrollView, Text } from 'react-native';

const TarefasTodosMembros = () => {
  return(
        <ScrollView>
            <Text>TODOS</Text>
        </ScrollView>  
    );
}

export default TarefasTodosMembros;